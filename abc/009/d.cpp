#include <bits/stdc++.h>

using namespace std;

using uint = unsigned int;

void multbool (uint* a, uint row, uint col){

  return;
}

int main(){
  uint k, m;
  cin >> k >> m;
  cin.ignore();

  uint a[k];
  uint c[k];

  for(uint i = 0; i < k; i++){
    cin >> a[i];
  }
  cin.ignore();

  for(uint i = 0; i < k; i++){
    cin >> c[i];
  }
  cin.ignore();

  
