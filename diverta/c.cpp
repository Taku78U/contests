#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main(){

  int n;
  cin >> n;
  cin.ignore();
  int a[n];

  for (int i = 0; i < n; i++){
    cin >> a[i];
  }
  cin.ignore();

  // 最大 - 最小 が最後に起きる
}
